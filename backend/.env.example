# Keep both values empty until Token Factory credits are redeemed.
NEBIUS_API_KEY=
# Set this only after choosing a NVIDIA Nemotron model in the authenticated Token Factory dashboard.
NEBIUS_MODEL=
NEBIUS_PROJECT_ID=
